# AR名刺システム 使い方ガイド

写真をマーカー(カード等)にかざすとAR上に名前・肩書き・SNSリンクが浮かび上がる仕組みの、運用マニュアルです。

- 公開ページ(お客様が見る側): `https://kakimittu.github.io/ar-test/?id=<ドキュメントID>`
- 編集ページ(管理者だけ): `https://kakimittu.github.io/ar-test/editor.html?id=<ドキュメントID>`
- データ保存先: Firebase プロジェクト `armisetai` の Firestore（コレクション `arUsers`、ドキュメントID = マーカーごとの識別子）

---

## 1. 全体の流れ

新しいお客様を1人追加する場合、以下の順番で作業します。

1. マーカー画像（顔写真や図柄など）を1枚用意する
2. その画像を MindAR の変換ツールで `.mind` ファイルに変換する（下記4章）
3. `.mind` ファイルをこのリポジトリの `assets/targets/` に置いて GitHub に push する
4. `editor.html` にログインし、新規ドキュメントIDを発行してプロフィール情報を入力・保存する
5. お客様に `index.html?id=<ドキュメントID>` のURL（またはQRコード）を渡す
6. お客様が上記URLをスマホで開き、①でマーカーに使った画像にカメラをかざすとARが表示される

**②③（.mind変換とアップロード）だけは自動化されていない手作業です。** それ以外(④⑤)はeditor.html上で完結します。

---

## 2. editor.html へのログイン

1. `https://kakimittu.github.io/ar-test/editor.html` を開く
2. ログイン画面が出るので、Firebase Console の Authentication に登録済みのメールアドレス／パスワードでログイン
3. 管理者を追加したい場合は Firebase Console →「Authentication」→「Users」→「ユーザーを追加」

ログインしていない状態では編集画面自体が表示されません（第三者が `editor.html?id=...` のURLを知っても中身を書き換えられないようにするためです）。

---

## 3. 新規のお客様を登録する（editor.html）

1. ログイン後、「ドキュメントID」欄の**「ランダム生成」ボタン**を押す
   - 推測されにくい8桁のランダムIDが自動生成され、フォームも新規入力用に空欄へリセットされます
   - すでに使っているIDを手入力することもできますが、他人に推測されると困る用途では基本的にランダム生成を使ってください
2. 「MindARターゲットURL (.mind)」欄に、4章で作った `.mind` ファイルのパスを入力
   - 例: `assets/targets/customer001.mind`
3. 「Profile Content」を入力
   - お名前 / 肩書き / フォント / お名前の色 / 肩書きの色 / アクセントカラー（肩書きの前に付く小さな四角の色）/ 文字サイズ
   - 右側のスマホプレビューにリアルタイムで反映されます
4. 「画像」欄から写真を選択（複数可。スマホでスキャンした後に左右スワイプ／タップで切り替わるカルーセルになります）
5. 「Social Links」に Instagram / X / HP / LINE のURLを入力（空欄のものはAR画面上でボタンごと非表示になります）
6. 「FIRESTOREへ保存」を押す

保存後、そのドキュメントIDが正式なお客様のURL識別子になります。控えておいてください。

---

## 4. マーカー画像を `.mind` ファイルに変換する（手作業）

MindARは事前にコンパイルされた `.mind` ファイルでしかマーカー認識できません。以下は公式のオンラインコンパイラを使う方法です。

1. https://hiukim.github.io/mind-ar-js-doc/tools/compile を開く
2. マーカーに使う画像（顔写真やロゴなど、模様がはっきりしていてコントラストの高い画像が認識精度が良い）をアップロード
3. 「Start Compiling」を実行し、完了したら `.mind` ファイルをダウンロード
4. ダウンロードした `.mind` を `ar-test-main/assets/targets/` フォルダに置く
5. Git でコミット & push（下記6章）
6. editor.html の「MindARターゲットURL」欄に `assets/targets/<ファイル名>.mind` を入力して保存

---

## 5. 既存のお客様情報を編集する

1. `editor.html?id=<そのお客様のドキュメントID>` を開く（URLにIDを付けて開くと自動で読み込まれます）
2. 起動時に既存のFirestoreデータが全項目に自動で読み込まれます
3. 変更したい項目だけ直して「FIRESTOREへ保存」

**写真について:** 「画像」欄で新しいファイルを選び直さない限り、既存の写真は保存時に消えません（テキストだけ直して保存しても写真はそのまま残ります）。写真を差し替えたい時だけ新しいファイルを選択してください。

---

## 6. サイトの更新を公開する（GitHub Pages）

このサイトは GitHub Pages（`kakimittu/ar-test` リポジトリの `main` ブランチ）から配信されています。

- `editor.html` でのプロフィール保存だけなら、Firestoreに直接書き込まれるのでコード変更・pushは不要です（保存した瞬間に公開ページへ反映されます）
- **コード自体（index.html / editor.html のデザインや機能）を変更した場合、または `.mind` ファイルなど新しいファイルを追加した場合のみ** git push が必要です

```powershell
git add <変更したファイル>
git commit -m "変更内容"
git push origin main
```

push後、GitHub Pagesの再ビルドに数十秒〜数分かかります。

---

## 7. お客様への提供物

- URL: `https://kakimittu.github.io/ar-test/?id=<ドキュメントID>`
- このURLをQRコード化して、マーカー画像を印刷した名刺・カードなどに添えて渡すのが基本的な運用です（QRコード生成はこのリポジトリの機能には含まれていないので、外部のQR生成ツールを使ってください）
- お客様がQRを読み取って上記URLを開き、カメラをマーカー画像にかざすとARコンテンツが表示されます
- `&edit=1` を付けた `https://kakimittu.github.io/ar-test/?id=<ID>&edit=1` を開くと画面右上に EDIT ボタンが出て `editor.html` に飛べます。管理者が現地で確認しながら編集したい時だけ使ってください（お客様に渡すURLには付けないこと）

---

## 8. 運用上の注意点（既知の制限）

- **写真サイズ**: 写真はBase64化してFirestoreドキュメントに直接保存しています。Firestoreは1ドキュメント最大1MiBの制限があるため、高解像度の写真を何枚も入れると保存エラーになることがあります。枚数は数枚程度に抑えるのが無難です。
- **`.mind`生成は手作業**: 3章の通り、お客様を追加するたびに手動でマーカー変換〜pushが必要です。件数が増えてきたら自動化を検討してください。
- **個人情報**: 氏名・顔写真・SNSリンクを扱うため、実際にサービスとして提供する場合は本人の同意取得やプライバシーポリシーの整備を推奨します。
- **管理者アカウント**: Firebase Console の Authentication で追加・削除・パスワードリセットができます。退職者が出た場合などは忘れずにアカウントを無効化してください。

---

## 9. うまくいかない時

| 症状 | 確認すること |
|---|---|
| editor.htmlでログインできない | Firebase Console → Authentication → Sign-in method で「メール/パスワード」が有効か。Usersタブにアカウントが存在するか |
| 保存時にエラーが出る | Firestoreのルールが `allow write: if request.auth != null;` になっているか（`firestore.rules` 参照）。写真サイズが大きすぎないか |
| GitHub Pagesに変更が反映されない | `git push` できているか（`git status`で確認）。ブラウザのキャッシュが残っていないか（Ctrl+Shift+Rで強制再読み込み） |
| ARでマーカーが認識されない | `.mind` ファイルのパスが正しいか。マーカー画像のコントラスト・模様の複雑さが十分か（単色に近い画像は認識精度が落ちます） |
| SNSボタンが表示されない | editor.htmlでそのURL欄が空になっていないか |
